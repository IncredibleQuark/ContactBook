{% extends "::base.html.twig"  %}
{% block title %}Contact {{ contact.id }}{% endblock %}


{%  block body %}
    <div class="container">
    <p>Name: {{ contact.name }}</p>
    <p>Surname: {{ contact.surname }}</p>
    <p>Description: {{ contact.description }}</p>
    <a href="{{ path('contactbook_contact_showallcontacts') }}">Main Page</a><br>
    <a href="{{ path('contactbook_contact_showeditform', {'id': contact.id}) }}">Edit</a><br>
    <a href="{{ path('contactbook_contact_deletecontact', {'id': contact.id}) }}">Delete</a>
    </div>
{% endblock %}